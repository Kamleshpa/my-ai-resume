import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Providers } from "@/components/providers";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Kamlesh Pandey — Lead Software Engineer | Interactive AI Resume",
  description:
    "Chat with my AI to learn about my 19+ years of experience building data platforms at Fortune 500 companies. An interactive resume powered by AI.",
  openGraph: {
    title: "Kamlesh Pandey — Interactive AI Resume",
    description:
      "Chat with my AI to learn about my 19+ years of experience building data platforms at Fortune 500 companies.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
